import gsap from "gsap";

export const slidePreset = {
  duration: 0.35,
  ease: "power2.out",
};

export const fadePreset = {
  duration: 0.25,
  ease: "power1.out",
};

export const smoothSpring = {
  duration: 0.6,
  ease: "elastic.out(1, 0.6)",
};

export const gentleShift = {
  duration: 0.3,
  ease: "power3.out",
};
